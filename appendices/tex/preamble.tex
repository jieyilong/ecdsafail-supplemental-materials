% Shared preamble for the standalone appendix documents.
% Mirrors the paper's own preamble so that typography, macros and
% cross-reference formatting are identical to the appendices as printed in the
% paper.  Kept in one place; each appendix-X.tex inputs it.
\def\fullversion{0}
\def\cameraversion{0}
\def\showoverflow{1}
\def\allow{0}      % 1 = show author notes (\jieyi{...}, \benhuang{...}), 0 = hide
\def\anonymous{0}
